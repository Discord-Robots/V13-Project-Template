const { Client, Guild } = require("discord.js");

module.exports = {
    name: "guildDelete",
    /**
     * 
     * @param {Client} client 
     * @param {Guild} guild 
     */
    async execute(client, guild) {
        
    }
}